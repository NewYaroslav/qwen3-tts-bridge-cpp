name: Python Checks

on:
  pull_request:
    paths:
      - ".github/workflows/python-checks.yml"
      - ".github/workflows/packaged-worker-smoke.yml"
      - "pyproject.toml"
      - "scripts/check-python.ps1"
      - "scripts/package-worker.ps1"
      - "scripts/setup-python-dev.ps1"
      - "scripts/setup-python-packaging.ps1"
      - "scripts/test-packaged-worker.ps1"
      - "tests/python/**"
      - "worker/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/python-checks.yml"
      - ".github/workflows/packaged-worker-smoke.yml"
      - "pyproject.toml"
      - "scripts/check-python.ps1"
      - "scripts/package-worker.ps1"
      - "scripts/setup-python-dev.ps1"
      - "scripts/setup-python-packaging.ps1"
      - "scripts/test-packaged-worker.ps1"
      - "tests/python/**"
      - "worker/**"

jobs:
  python:
    name: Python worker
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dev tooling
        shell: pwsh
        run: .\scripts\setup-python-dev.ps1 -Python python

      - name: Run Python checks
        shell: pwsh
        run: .\scripts\check-python.ps1 -Python python

      - name: Validate worker packaging command
        shell: pwsh
        run: .\scripts\package-worker.ps1 -Python python -DryRun
